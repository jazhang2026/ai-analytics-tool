# Backlog List

## Add test-plan and test-run skills

- [x] Create `speckit-test-plan` skill (plans unit + integration test cases)
- [x] Create `speckit-test-run` skill (writes and runs unit + integration tests)
- [x] Remove duplicate `devin-test` skill

## Add and run unit tests for frontend and backend

- [x] Backend unit tests (`test_auth.py`, `test_models.py`, `test_seed_operator.py`) — 25 passing
- [ ] Frontend unit tests (not yet configured)

## Add and run integration tests

- [x] Backend integration tests (`test_api.py`) — 19 passing
- [ ] Frontend E2E / browser tests (deferred — manual UI verification per quickstart.md)
